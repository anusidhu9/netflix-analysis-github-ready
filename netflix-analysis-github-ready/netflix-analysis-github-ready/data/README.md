# Data

Place the Netflix CSV dataset required by `notebooks/netflix-analysis.ipynb` in this folder.

The uploaded notebook did not include the original dataset, so the CSV itself is intentionally not included in this project package.
